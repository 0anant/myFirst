const JSXRules = () => {
  return (
    <div>
        <h1>
            "JSX Rules"
        </h1>
        <p>
        <ol>
            <li>JSX must return a single parent</li>
            <li>JSX elements must be properly closed</li>
            <li>JSX attributes are written using camel case</li>
            <li>in JSX instead of using for, use htmlFor in Forms(labels)</li>
            <li>in JSX instesd of using class, className instead</li>
        </ol>
        </p>
    </div>
  )
}

export default JSXRules